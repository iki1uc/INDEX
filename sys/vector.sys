AUTO.sys.VECTOR = {
  auto:    AUTO.LIST.map(v => VECTOR_MEASURE(v.vec)),
  vector:  VECTOR.map(v => VECTOR_MEASURE(v)),
  vec128:  VEC128.map(v => VECTOR_MEASURE(v.vec128)),
  run360:  RUN360_VECTOR.map(v => VECTOR_MEASURE(v)),
  run128:  RUN128_VECTOR.map(v => VECTOR_MEASURE(v)),
  run12:   RUN12_VECTOR.map(v => VECTOR_MEASURE(v)),
  run9:    RUN9_VECTOR.map(v => VECTOR_MEASURE(v)),
  matrix81: MATRIX81_VECTOR.map(v => VECTOR_MEASURE(v))
};
